services:
  kaede:
    build:
      context: ./apps/kaede
    image: okarin/kaede:production
    env_file:
      - ${ENV_FILE:-./deploy/env.production}

  nozomi:
    build:
      context: ./apps/nozomi
    image: okarin/nozomi:production
    env_file:
      - ${ENV_FILE:-./deploy/env.production}
